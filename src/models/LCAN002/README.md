The following code snippet is structured according to the available css of `test`.

You may copy it and paste it into your own theme typescript file before editing it.

---

```
const StyledView = {
  ".container": {
    background: "blue",
  },
};

export default StyledView;

```
